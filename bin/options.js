'use strict';

const MODULE_REQUIRE = 1
	/* built-in */
	
	/* NPM */
	, noda = require('noda')
	
	/* in-package */
	;

module.exports = {
	"options": [
		'--help -h NOT ASSIGNABLE',
		'--origin [0] NOT NULL',
		'--name [1] NOT NULL',
		'--reset',
		'--apply',
	]
};